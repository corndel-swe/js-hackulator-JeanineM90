/**
 * Returns the product
 * multiply(2, 3) => 6
 */
function multiply(x, y) {
  const result = X * y
  return result
}

export default multiply

/**multiply function
    25) should return the product of two positive numbers
    26) should return the product of a positive and a negative number
    27) should return the product of two negative numbers
    28) should return 0 when one of the arguments is 0
    29) should return the product of a number and 1
    30) should return the product for larger numbers
    */
